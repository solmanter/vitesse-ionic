import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.unnamed',
  appName: 'unnamed',
  webDir: 'dist',
  server: {
    androidScheme: 'http',
  },
}

export default config
